#include "header.h"

int main() {

   Rectangle rect(5,2);
   Elipse elip(5,2);
   Shape shap(5,2);
   RegularShape regSha(5);

   regSha.getSurface();
   regSha.getVolume();

   Rectangle rectc(rect);
   Elipse elipc(elip);
   Shape shapc(shap);
   RegularShape regShac(regSha);   

   return 0;
}
